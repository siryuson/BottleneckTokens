# SUN397

> 大规模场景识别数据集，在 MMEB 训练中用于增强细粒度场景语义区分能力。

| 属性 | 值 |
|------|-----|
| 模态 | image |
| 任务类型 | `image_classification` |
| 用途 | 训练 |
| 溯源层数 | 3 层 |

## 原始数据集

### 来源信息
| 属性 | 值 |
|------|-----|
| 原始名称 | SUN397 |
| 论文 | SUN Database: Exploring a Large Collection of Scene Categories ([arXiv](https://arxiv.org/abs/1004.4685)) |
| 主要作者 | Jianxiong Xiao, James Hays, Krista A. Ehinger, Aude Oliva, Antonio Torralba |
| HuggingFace | [flwrlabs/sun397](https://huggingface.co/datasets/flwrlabs/sun397) |
| 许可证 | 参见原始数据集 |

### Schema
原始样本为场景图像及场景类别标签，覆盖大量自然与人造环境类别。

### 样本

**查询文本**: `<|image_pad|>...`
**正样本文本**: `abbey`
**负样本文本**: ``

---

## MMEB-train（VLM2Vec 转换后）

### 来源信息
| 属性 | 值 |
|------|-----|
| HuggingFace | [siyrus/BToks-MMEB-train](https://huggingface.co/datasets/siyrus/BToks-MMEB-train) |
| 子集名 | `SUN397` |
| 分割 | `train` |

### Schema
`qry_inst`, `qry_text`, `qry_img_path`, `pos_text`, `pos_img_path`, `neg_text`, `neg_img_path`。

### 样本

**查询文本**: `<|image_pad|>...`
**正样本文本**: `abbey`
**负样本文本**: ``

---

## BToks Lance 格式

### 来源信息
| 属性 | 值 |
|------|-----|
| 数据加载器 | `mmeb_train` |
| 实现文件 | `src/vlm2emb/data/datasets/mmeb_train.py` |
| 注册名 | `mmeb_train` |
| sample 表路径 | `./data/converted` |
| 图像表路径 | `./data/converted` |

### Lance 布局

**sample 表 `data/SUN397/train.lance`**:

| 列名 | 类型 | 说明 |
|------|------|------|
| qry | string | 查询文本（原始历史 token 仍可能是 `<|image_1|>`） |
| qry_image_path | string | 查询图像路径 |
| pos_text | string | 正样本文本 |
| pos_image_path | string | 正样本图像路径，可为空 |
| neg_text | string | 负样本文本，可为空 |
| neg_image_path | string | 负样本图像路径，可为空 |

**图像表 `data/images/SUN397.lance`**:

| 列名 | 类型 | 说明 |
|------|------|------|
| path | string | 原始图片路径 |
| image | binary | 图像二进制 |

### 训练侧映射
- query: `qry` + `qry_image_path` 对应图像（若有）
- positive: `pos_text` + `pos_image_path` 对应图像（若有）
- negative: `neg_text` + `neg_image_path` 对应图像（若有）
- runtime 默认会把历史 `<|image_1|>` 归一成 `<|image_pad|>`

### 样本

以下示例直接采样自真实训练 Lance 表。

- sample 行保留 Lance 原值。
- 图像行中的 `image` 按 `bytes / sha1 / format / size` 展示摘要，不直接展开原始二进制。

#### 示例 1

```json
{
  "data/SUN397/train.lance": {
    "qry": "<|image_1|>\nIdentify the scene shown in the image\n",
    "qry_image_path": "images/SUN397/Train/image_0.jpg",
    "pos_text": "abbey",
    "pos_image_path": "",
    "neg_text": "",
    "neg_image_path": ""
  },
  "data/images/SUN397.lance": {
    "query_image": {
      "path": "images/SUN397/Train/image_0.jpg",
      "data": {
        "bytes": 16713,
        "sha1": "a931f94711c4",
        "format": "JPEG",
        "size": [
          234,
          205
        ]
      }
    }
  }
}
```

#### 示例 2

```json
{
  "data/SUN397/train.lance": {
    "qry": "<|image_1|>\nIdentify the scene shown in the image\n",
    "qry_image_path": "images/SUN397/Train/image_9925.jpg",
    "pos_text": "pilothouse indoor",
    "pos_image_path": "",
    "neg_text": "",
    "neg_image_path": ""
  },
  "data/images/SUN397.lance": {
    "query_image": {
      "path": "images/SUN397/Train/image_9925.jpg",
      "data": {
        "bytes": 8362,
        "sha1": "767055fe4bb5",
        "format": "JPEG",
        "size": [
          212,
          283
        ]
      }
    }
  }
}
```
